<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bitflash.dao.UserComplaintDao">

    <select id="getComplaintMessage" resultType="cn.bitflash.trade.UserComplaintBean">
        select
        uh.*,uc.*,ui.mobile,ui.nickname
        from user_complaint uc
        left join user_buy_history uh on uh.user_buy_id = uc.order_id
        left join user_info ui on uh.purchase_id = ui.uid
        where uc.order_id = #{id}
    </select>

</mapper>